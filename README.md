# Claude Scheduler

Claude Scheduler is a cross platform tool for scheduling tasks using Claude Code. You can configure MCP servers skills and plugins
and configure prompts to run at specific times or intervals.

Calude Scheduler has a full UI to show the previous runs of a scheduled task along with their outputs and any errors that may have occurred.